<div class="row g-3">
    <div class="col-12">
        <div class="card card-body shadow-sm mb-2">
            <h3 class="mb-2">Персональные рекомендации</h3>
            <p class="text-muted mb-0">
                Пользователь: <strong><%= email || 'не указан' %></strong>
                (<code><%= userId %></code>). Данные берём из последних действий: просмотров, лайков, покупок.
            </p>
        </div>
    </div>
    <div class="col-12">
        <div id="reco_list" class="row g-3">
            <div class="col-12 text-muted" id="reco_status">Загрузка...</div>
        </div>
    </div>
</div>

<script>
    window.__USER_ID__ = "<%= userId %>";
    window.__RECO_PAGE__ = true;
</script>
